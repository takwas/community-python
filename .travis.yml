language: python
python:
  - "3.5"
services:
  - mysql

before_install:
  - mysql -u root -e 'CREATE DATABASE woodys_platform;'

install: "pip install -r requirements.txt"
# command to run tests
script: "python test.py"